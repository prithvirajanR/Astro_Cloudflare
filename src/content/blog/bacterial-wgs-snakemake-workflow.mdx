---
title: "Bacterial WGS workflow design with Snakemake"
description: "A technical note on a bacterial whole-genome sequencing workflow that moves from raw reads to assembly, polishing, quality control, annotation, optional screening, and phylogeny."
date: 2025-03-14
tags: ["Snakemake", "Bacterial WGS", "Genomics", "Reproducibility", "Conda"]
github: "https://github.com/prithvirajanR/bacteria-genome-analysis-workflow"
repo: "bacteria-genome-analysis-workflow"
heroImage: "/blog/bactoflow-workflow.svg"
heroAlt: "Bacterial WGS workflow diagram from raw reads through QC, assembly, annotation, screening, and reports"
readingTime: "6 min read"
featured: true
order: 4
---

This repository defines a Snakemake workflow for bacterial whole-genome sequencing analysis. The workflow is designed around a common need in microbial genomics: turning paired-end reads, and optionally long reads, into assemblies, quality reports, annotations, screening outputs, and phylogenetic summaries.

The value of the project is not a new algorithm. It is the explicit connection of many established tools into a reproducible workflow with declared inputs, environments, and output targets.

## Workflow design

The workflow starts from a sample sheet and chooses the assembly path based on whether long reads are available.

Samples with long reads go through a hybrid assembly route. Samples without long reads use a short-read assembly route. After assembly, the pipeline moves into polishing, quality control, annotation, screening, comparative genomics, and reporting.

The final targets include:

- MultiQC reports
- long-read QC summaries
- QUAST reports
- BUSCO summaries
- Bakta annotation GFF3 files
- optional MLST, AMR, virulence, and plasmid screening outputs
- a screening summary Excel report
- core-genome tree files and visualizations

The output design matters because microbial genomics results are often reviewed by people with different preferences: command-line users, report readers, and collaborators who need spreadsheet summaries.

## Why Snakemake fits the problem

Snakemake gives the project a clear contract: what files go in, what files come out, and which rule creates each output. Conda environments are split per module, so tools for QC, assembly, annotation, screening, and reporting can be managed separately.

That matters in bacterial genomics because workflows tend to become fragile when every tool is installed into one environment and every sample is handled manually.

## Evidence and outputs

The workflow diagram and repository structure document the expected analysis path. The final targets include QC reports, assemblies, annotations, screening summaries, and core-genome phylogeny files.

Even though raw FASTQ files and generated results are not tracked in Git, the pipeline records the expected outputs in the Snakefile. That makes the repo understandable without shipping huge data files.

## Limitations and next steps

The next version could add a small public test dataset and a CI dry-run so new users can verify the workflow immediately. I would also add example rendered reports to the site so the output structure is visible without running the full pipeline.

As a research-engineering artifact, the repository demonstrates modular rules, clear configuration, sample-sheet driven execution, and outputs that support both analysis and review.
